name: Build and Deploy
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
      - name: Cache node_modules
        id: cache-node-modules
        uses: actions/cache@v2
        with:
          path: node_modules
          key: ${{ runner.os }}-nodeModules-${{ hashFiles('package-lock.json') }}-${{ hashFiles('package.json') }}
          restore-keys: | 
            ${{ runner.os }}-nodeModules-
      - name: Install 🔧
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: |-
          npm install
          cat README.md
      - name: Build and Deploy dist🔧
        run: npm run deploy
      - name: Add and Commit files
        uses: EndBug/add-and-commit@v9
        with:
          default_author: github_actions
      # - name: Commit and push if changed
      #   run: |-
      #     git diff
      #     git config --global user.email "deuscx@gmail.com"
      #     git config --global user.name "deuscx"
      #     git branch -u origin/main
      #     git pull
      #     git add -A
      #     git commit -m "Updated content" || exit 0
      #     git push -u origin/main
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.4
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: src/dist # The folder the action should deploy.
          # target-folder: ${{ env.slide }}
          # clean: false

      # - name: Update README
      #   run: |
      #     echo "|${{ env.slide }}|-|-|[link](https://guisso.dev/talks/${{ env.slide }}/)|" >> README.md
      # - name: Commit README changes
      #   uses: stefanzweifel/git-auto-commit-action@v4
      #   with:
      #     commit_message: "readme: add slide link"
      #     file_pattern: README.md
      # - name: Checkout for README 🛎️
      #   uses: actions/checkout@v2